<style lang="scss" is:global>
  #post {
    padding-top: 70px;

    @media (min-width: 992px) {
      padding-top: 78px;
    }

    @media (max-width: 992px) {
      --text: 16px;
    }
  }

  #post img,
  .project-gallery__image {
    width: 100%;
    height: auto;
  }

  .item-grid-5 {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 0 20px;

    @media (max-width: 1100px) {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .item-grid-5--start { align-items: start; }
  .item-grid-5--end { align-items: end; }
  .item-grid-5--center { align-items: center; }

  .project-content {
    margin-top: 80px;

    @media (max-width: 767px) {
      margin-top: 60px;
    }
  }

  .project-content__info__inner {
    position: sticky;
    top: 70px;
    padding-top: 72px;
  }

  .project-content__next {
    font-size: clamp(16px, 8vw, 30px);
    font-weight: 600;

    strong {
      display: block;
      color: var(--color-0);
      font-size: clamp(36px, 14.666vw, 60px);
      font-weight: bold;
    }
  }

  .project-detail__item {
    margin-bottom: 40px;
    font-size: var(--text);

    @media (max-width: 1100px) {
      margin-bottom: 20px;
    }
  }

  .project-detail__label {
    font-size: 0.8em;
    font-weight: 400;
    line-height: 1.8;
  }

  .project-detail__value {
    grid-column: span 4;
    font-size: 1em;
    font-weight: 600;
    line-height: 1.6;

    @media (max-width: 1100px) {
      grid-column: span 3;
    }
  }

  .project-gallery {
    padding: 10vh 0 10vw;

    @media (min-width: 1200px) {
      padding-top: 20vh;
    }
  }

  .project-gallery__item {
    margin-bottom: 20px;
  }
</style>
