services:
  tink:
    build: .
    container_name: tink-photography
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-4321}:4321"
    volumes:
      - ${DOCKER_DATA_DIR:-./data}:/app/data
      - ${DOCKER_BACKUP_DIR:-./backups}:/app/backups
