<style lang="scss" is:global>
  .collection {
    &-item-double {
      position: relative;
      display: flex;
      flex-direction: row;
      align-items: center;
    }

    &-item {
      position: relative;
      display: flex;
      flex-direction: row;
      align-items: center;
      margin-bottom: 6vh;

      @media (min-width: 767px) {
        margin-bottom: 12vw;
      }
    }

    &__figure {
      position: relative;
    }

    &__img {
      position: relative;
      width: 100%;
      max-width: 100%;
      height: auto;
      display: block;
    }
  }
</style>
